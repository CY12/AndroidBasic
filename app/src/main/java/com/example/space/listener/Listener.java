package com.example.space.listener;

public class Listener {

    /**
     * public interface onClickListener {
     *         void onClick(String location);
     *     }
     *
     *     onClickListener mOnClickListener;
     *
     *     public void setmOnClickListener(onClickListener onClickListener) {
     *         mOnClickListener = onClickListener;
     *     }
     *
     *
     *     if (mOnClickListener != null) {
     *                         mOnClickListener.onClick(selected.getLocation());
     *                     }
     *
     * adapter.setmOnClickListener(new CityRecyclerAdapter.onClickListener() {
     *             @Override
     *             public void onClick(String location) {
     *                 Intent intent = new Intent(MainActivity.this, DetailWeather.class);
     *                 intent.putExtra("Location", location);
     *                 startActivity(intent);
     *                 showProgress();
     *
     *             }
     *         });
     *
     */
}
